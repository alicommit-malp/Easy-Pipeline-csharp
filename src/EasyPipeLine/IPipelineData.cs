namespace EasyPipeLine
{
    /// <summary>
    /// Any class which implement <see cref="IPipelineData"/> can be used as the data which is being passed between the
    /// <see cref="WorkStation"/> classes 
    /// </summary>
    public interface IPipelineData
    {
    }
}